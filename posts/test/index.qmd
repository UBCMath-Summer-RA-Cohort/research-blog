---
title: "Testing for Demo"
author: "Riddhi Battu"
date: "2026-05-27"
description: "Demo on how to create a post"
categories: [demo, quarto]
jupyter: ubcmath-ra-blog
---

This is a test post to demonstrate how to create a new blog post using Quarto.

# This is a heading
Here is some sample content for the blog post. You can write in Markdown and include code chunks as needed.
```{python}
# This is a Python code chunk
import numpy as np
import matplotlib.pyplot as plt 
x = np.linspace(0, 10, 100)
y = np.sin(x)
plt.plot(x, y)
plt.title("Sine Wave")
plt.show()

# load csv
import pandas as pd
df = pd.read_csv('data/demo.csv')
print(df.head())
print(f'df.describe():\n{df.describe()}')
```

The dataset has `{python} len(df)` rows.
The dataset has `{python} len(df.columns)` columns. The columns are: `{python} df.columns.tolist()`.

You can also include mathematical equations using LaTeX syntax. For example, the quadratic formula is given by:
$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$ 

